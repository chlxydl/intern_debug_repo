# task374_repo_dotdir_validation

<!-- METADATA:STATUS=Open,ASSIGNEE= -->

## 背景

主管要求创建一个任务：在 README.md 中添加一行 `task374 repo_dotdir validation`。

## 任务目标

在仓库根目录的 `README.md` 中新增一行：

```text
task374 repo_dotdir validation
```

## 验收标准

- `README.md` 包含独立一行 `task374 repo_dotdir validation`。
- 代码修改通过任务分支和 PR 流程提交，不直接 push 到默认分支。
