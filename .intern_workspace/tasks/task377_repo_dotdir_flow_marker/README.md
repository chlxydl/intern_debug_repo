# task377_repo_dotdir_flow_marker

<!-- METADATA:STATUS=Open,ASSIGNEE= -->

## 背景

主管要求创建一个任务，用于在仓库根目录 `README.md` 中追加 repo_dotdir 流程标记。

## 任务目标

在仓库根目录的 `README.md` 中追加一行：

```text
task377 repo_dotdir flow marker
```

## 验收标准

- 代码修改通过任务分支和 PR 流程提交。
- `README.md` 中存在一行 `task377 repo_dotdir flow marker`。
- 该修改仅追加 repo_dotdir 流程标记，不影响项目运行逻辑。
