# task374_repo_dotdir_playbook_marker - Task Knowledge

<!-- METADATA:SESSION=2 -->

## Knowledge Entries

- Workspace metadata mode is repo_dotdir; task metadata belongs under `.intern_workspace/tasks/`.
- Creating task metadata may be committed directly from the latest default branch, while code changes still require a task branch and PR.
- Existing E2E validation markers are plain text files under `e2e/`, so a new isolated marker file there matches the repository pattern.
